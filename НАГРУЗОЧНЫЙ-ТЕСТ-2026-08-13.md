# Нагрузочный тест и подбор сервера для аренды

13.08.2026. Это не расчёт на бумаге, а замеры на стенде: поднят PostgreSQL, схема, индексы,
ограничения, политики RLS и функции сняты с боевой базы дословно, сверху поставлен настоящий
PostgREST — тот же слой, через который ходит приложение. Боевая база не нагружалась,
из неё только читалась структура.

---

## Короткий ответ

**1000 человек одновременно — это 126 запросов в секунду и 0,28 ядра.** Не «сервер на пределе»,
а четырнадцать процентов от двух слабых ядер стенда. Проект в этом смысле лёгкий: он опрашивает
базу по таймеру короткими запросами, тяжёлых отчётов и аналитики в нём нет.

**Узкое место — не процессор, а оперативная память**, и не из-за приложения, а из-за самого
self-hosted Supabase: полтора десятка контейнеров съедают 4–6 ГБ в покое, ещё не обслужив
ни одного пользователя.

**Рекомендую тариф «Отрыв»: 4 ядра / 8 ГБ / 80 ГБ — 1379 ₽/мес.** Это не «впритык», а с запасом
кратно: по замерам такой сервер вывезет 4000–5000 одновременных вкладок. Брать дороже сейчас
нечего — деньги уйдут в простаивающее железо.

**Но сначала добавьте один индекс.** Он снижает стоимость главного запроса приложения втрое
и — что важнее — делает её независимой от того, сколько поездок накопится в базе. Без него
сервис деградирует по мере роста, и никакое железо это не лечит.

---

## 1. Как устроен стенд

| Что | Как воспроизведено |
|---|---|
| Схема, индексы, CHECK-ограничения | Сняты с боевой базы запросами к системному каталогу, применены дословно |
| Политики RLS | Все 18 политик перенесены как есть, включены на всех таблицах |
| `auth.uid()` | Заглушка, читающая пользователя из настройки запроса — ровно как это делает PostgREST |
| Функции `get_trip_view`, `place_bid`, `finish_auction`, `fmt_money` | Скопированы с боевой базы без изменений |
| HTTP-слой | Настоящий PostgREST 12.2.12 с JWT-авторизацией |
| Запросы | Собраны из кода: `Feed.tsx`, `MainLayout.tsx`, `TripDetail.tsx` |

**Железо стенда: 2 ядра Xeon 2.1 ГГц, 8 ГБ.** Это заведомо слабее того, что даёт FirstVDS
(линейка CPU.Турбо разгоняется до 5,7 ГГц), поэтому все цифры ниже — пессимистичные.

**Объём данных — «сервис проработал год»:** 50 000 пользователей, 60 000 поездок (2 000 активных),
265 000 ставок, 400 000 уведомлений, 20 000 отзывов. База 240 МБ.

Две оговорки, честные: на стенде PostgreSQL 16, в бою 17 — разница в производительности этих
запросов пренебрежимая. И стенд не включает GoTrue, Realtime, Storage и Kong; их вклад оценён
отдельно в разделе 5, потому что он определяется не запросами, а фактом их запуска.

---

## 2. Что и как часто дёргает базу

Частоты взяты не с потолка, а из кода:

| Запрос | Откуда | Частота на одну вкладку |
|---|---|---|
| Лента поездок | `Feed.tsx`, опрос вместо realtime | раз в 15 секунд |
| Новые уведомления | `MainLayout.tsx` | раз в 20 секунд |
| Карточка поездки `get_trip_view` | `TripDetail.tsx` | ~раз в 2 минуты |
| Ставка `place_bid` | `TripDetail.tsx` | ~1 на 1000 вкладок в секунду |

Опрос останавливается, когда вкладка скрыта (`document.hidden`) — это уже сделано и заметно
экономит: реально активных вкладок всегда меньше открытых.

---

## 3. Замеры: цена одного запроса

Считалось процессорное время сервера (PostgreSQL + PostgREST) на один HTTP-запрос — генератор
нагрузки из подсчёта исключён.

| Запрос | CPU на запрос | Задержка p50 | Задержка p99 | Размер ответа |
|---|---|---|---|---|
| Лента, **как сейчас** | **10,30 мс** | 41,0 мс | 71,8 мс | 33 КБ |
| Лента, **с индексом** | **3,47 мс** | 15,6 мс | 34,6 мс | 33 КБ |
| Лента с индексом, активных поездок 20 000 | **3,51 мс** | 15,6 мс | 33,0 мс | 33 КБ |
| Уведомления | 0,84 мс | 3,7 мс | 8,2 мс | 0,4 КБ |
| `get_trip_view` | 1,00 мс | 4,2 мс | 9,3 мс | 5,3 КБ |
| `place_bid` (запись) | 3,20 мс | — | — | 0,2 КБ |

Третья строка — самая важная. С индексом стоимость ленты **не изменилась при десятикратном росте
числа активных поездок**: 3,47 → 3,51 мс. Без индекса она растёт вместе с базой линейно.

---

## 4. Сколько это в пересчёте на людей

| Одновременных вкладок | Запросов/с | Ядер сейчас | Ядер с индексом | Трафик |
|---|---|---|---|---|
| 50 | 6 | 0,04 | 0,01 | 0,9 Мбит/с |
| 200 | 25 | 0,15 | 0,06 | 3,5 Мбит/с |
| 500 | 63 | 0,37 | 0,14 | 8,8 Мбит/с |
| **1000** | **126** | **0,74** | **0,28** | **17,7 Мбит/с** |
| 2000 | 252 | 1,48 | 0,57 | 35 Мбит/с |
| 3000 | 378 | 2,22 | 0,85 | 53 Мбит/с |
| 5000 | 630 | 3,70 | 1,42 | 88 Мбит/с |

Расчёт проверен прямой эмуляцией: 1000 виртуальных вкладок с настоящими частотами опроса дали
**0,28 ядра и 15,1 Мбит/с** — сошлось с моделью.

**1000 человек в сутки** — это совсем другая величина: пик обычно 30–80 одновременных вкладок,
то есть 4–10 запросов в секунду и сотые доли ядра. Такой нагрузки сервер просто не замечает.

Обратите внимание на правый столбец. **Раньше процессора кончится канал.** У FirstVDS во всех
тарифах 100 Мбит/с, и на 5000 вкладок лента одна съедает 88 из них. Причина — размер ответа:
50 поездок со всеми полями и вложенным автором это 33 КБ на каждый опрос каждой вкладки
каждые 15 секунд.

---

## 5. Память: здесь и находится настоящее ограничение

Замерено на стенде под нагрузкой: PostgreSQL — 1,0 ГБ, PostgREST — 37 МБ. Всё остальное
в self-hosted Supabase к замеру не относится, но память съест:

| Слой | Память |
|---|---|
| PostgreSQL (при `shared_buffers` 2 ГБ) | 2,0–3,0 ГБ |
| PostgREST | ~50 МБ |
| GoTrue (авторизация) | 100–200 МБ |
| Realtime (Elixir) | 300–500 МБ |
| Storage + imgproxy (аватары, фото машин) | 200–400 МБ |
| Kong (шлюз) | 200–300 МБ |
| postgres-meta | ~100 МБ |
| nginx со статикой | ~50 МБ |
| **Итого рабочий минимум** | **3,5–4,5 ГБ** |
| Studio + analytics (Logflare) + vector | ещё 1,0–1,5 ГБ |

**Analytics отключить, Studio наружу не публиковать** — это не экономия ради экономии, Logflare
самый прожорливый контейнер в сборке и на старте не нужен. Со включённым analytics 8 ГБ
превращаются в «впритык», без него — в комфортные.

Обязательно **swap 2–4 ГБ**: не для работы, а чтобы всплеск не убил контейнер по OOM.

---

## 6. Диск

Замерено на объёме «год работы» (50 тыс. пользователей, 60 тыс. поездок): **240 МБ**.

| Таблица | Размер | На строку |
|---|---|---|
| `notifications` | 103 МБ (400 000) | 270 Б |
| `bids` | 55 МБ (266 000) | 220 Б |
| `rides` | 38 МБ (60 000) | 660 Б |
| `users` | 15 МБ (50 000) | 314 Б |

Даже при десятикратном росте это 2,4 ГБ. Реально диск съедят не данные, а аватары и фото машин
в Storage плюс журналы: 10 000 пользователей с картинкой по 100 КБ — это 1 ГБ.

**80 ГБ хватит на годы**, при двух условиях: настроена ротация журналов и очистка WAL.
Заполнившийся диск роняет PostgreSQL — за местом надо следить.

Отдельно: `notifications` — самая большая таблица, 43% базы. В проекте уже есть миграция
`20260604_retention_cleanup.sql`; убедитесь, что после переезда задача `pg_cron` для неё
пересоздана, иначе таблица будет расти вечно.

---

## 7. Что взять на FirstVDS

Цены с 1 марта 2026 года (провайдер поднял их примерно на 15%), готовые конфигурации:

| Тариф | CPU | RAM | Диск | Цена | Вердикт |
|---|---|---|---|---|---|
| Разгон | 2 | 4 ГБ | 60 ГБ SSD | 779 ₽ | По процессору хватит и на 3000 вкладок. **Не хватит памяти** под полный Supabase |
| **Отрыв** | **4** | **8 ГБ** | **80 ГБ SSD** | **1379 ₽** | **Рекомендую.** Память с запасом, процессор простаивает, диска на годы |
| Улёт | 8 | 12 ГБ | 120 ГБ SSD | 2299 ₽ | Избыточно на сейчас. Сюда переходить, когда 8 ГБ станет тесно |

Есть ещё конфигураторы с NVMe — «CPU.Турбо 2.0» от 799 ₽ и «Форсаж NVMe» от 1099 ₽, где
ядра, память и диск набираются вручную. Если ценник на 4/8/80 там окажется сопоставим с «Отрывом» —
берите их: NVMe и высокая частота ядра дадут запас по задержкам почти бесплатно. Цены меняются,
итоговую сумму сверьте в калькуляторе на сайте.

**Дата-центр берите московский** (WEB DC или IXcellerate). Это не вопрос скорости: проект
обрабатывает персональные данные граждан РФ, а сейчас база физически в Австралии — это нарушение
152-ФЗ уже сегодня. Амстердам и Алматы проблему не решают.

Канал 100 Мбит/с одинаков во всех тарифах — при росте выше 3000 одновременных вкладок он станет
ограничителем раньше процессора (см. раздел 4).

---

## 8. Что сделать до переезда

### Обязательно: индекс для ленты

```sql
create index concurrently idx_rides_feed
  on public.rides (type, created_at desc)
  where status in ('active', 'cancelled');
```

Что меняется в плане запроса: сейчас PostgreSQL поднимает **все** подходящие поездки (на стенде
1342 строки), для каждой лезет в `users` за автором, сортирует всё это и выбрасывает всё, кроме
первых пятидесяти — 5027 обращений к буферам на один опрос ленты. С индексом он берёт ровно
50 строк уже в нужном порядке: 219 обращений вместо 5027, 3,47 мс вместо 10,30.

`concurrently` — чтобы не блокировать таблицу на боевой базе.

Индексы `idx_rides_status` и `idx_rides_type` после этого станут почти бесполезными, но удалять
их сразу не нужно: сначала неделю посмотрите на `pg_stat_user_indexes`.

### Стоит сделать: облегчить ответ ленты

33 КБ на опрос — это в основном поле `comment` (до 1000 символов на поездку) и полный набор
полей автора. В списке комментарий целиком не нужен, достаточно первых строк. Урезание ответа
вдвое вдвое же снижает и трафик, и время сериализации. Это единственное, что реально упирается
в предел тарифа на горизонте роста.

### Настройки PostgreSQL под 4 ядра / 8 ГБ

```conf
shared_buffers = 2GB              # база целиком помещается в память ещё долго
effective_cache_size = 5GB
work_mem = 16MB
maintenance_work_mem = 512MB
max_connections = 100             # больше не нужно: PostgREST держит свой пул
random_page_cost = 1.1            # SSD/NVMe, а не диск с блинами
effective_io_concurrency = 200
track_io_timing = on
shared_preload_libraries = 'pg_stat_statements'
```

`max_connections = 100` — не жадность. Каждое соединение это отдельный процесс и своя память;
1000 пользователей не означают 1000 соединений, они мультиплексируются пулом PostgREST
(по умолчанию 10, поставьте 20–30). Supavisor в сборке есть, но при таком профиле он не нужен.

### Не забыть при переезде

1. **Бэкапы настроить первым делом и проверить восстановление в тот же день.** В облаке они были
   автоматические, на своём сервере их не будет, пока не сделаете. Непроверенный бэкап
   бэкапом не является.
2. Пересоздать задачи `pg_cron` — включая очистку уведомлений, иначе таблица растёт вечно.
3. Заменить домен Supabase в CSP (`vercel.json` → конфиг nginx). Забудете — сайт откроется,
   но не сможет обратиться к базе.
4. Мониторинг: место на диске, живость контейнеров, доступность вебхука ЮMoney.

---

## 9. Чего этот тест не проверял

Честно о границах: измерен путь «браузер → PostgREST → PostgreSQL», то есть всё, что создаёт
нагрузку в обычной работе. Вне замера остались:

- **Realtime.** На странице поездки открываются два канала (`TripDetail.tsx`). Если 15% из 1000
  вкладок сидят на карточке — это ~300 постоянных websocket-соединений. Для Elixir это немного,
  но память под них считайте по верхней границе таблицы из раздела 5.
- **GoTrue.** Обновление токена раз в час на пользователя — при 1000 активных это 0,3 запроса
  в секунду. Пренебрежимо, но при массовом одновременном входе (реклама, рассылка) будет всплеск.
- **Storage.** Загрузка аватаров и фото машин — редкая операция, но каждая тяжёлая по трафику.
- **Отдача статики.** `dist` весит 1,6 МБ (около 450 КБ в сжатом виде). nginx на этом железе
  отдаёт такое тысячами запросов в секунду, но 1000 первых заходов подряд — это 450 МБ трафика.
  Настройте `gzip`/`brotli` и заголовки кэширования — они уже есть в `vercel.json`, перенесите
  их в конфиг nginx.
