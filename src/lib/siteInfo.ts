/**
 * Публичные реквизиты сервиса (инфо-страницы, оферта, политика, квитанция).
 */
export const SITE = {
  name: 'APSNY-TRANSFER',
  tagline: 'Умный агрегатор трансферов между Абхазией и Россией',

  // Единый контакт для всех обращений (поддержка, оплата, ПДн):
  email: 'mincorey@internet.ru',

  // Дата вступления документов в силу. Меняется вместе с текстами документов —
  // если поправили оферту или политику, поправьте и её, иначе документ выглядит
  // устаревшим: до 14.08.2026 здесь стояло 13 июня, хотя тексты переписывались
  // в августе.
  legalDate: '14 августа 2026 г.',
} as const;

/**
 * Реквизиты исполнителя — того, кто принимает оплату и отвечает по договору.
 *
 * Раньше их здесь не было намеренно: сервис не брал денег, а публиковать личные
 * данные владельца без нужды незачем. С оформлением самозанятости положение
 * изменилось. Возмездный договор заключается с конкретным человеком, и клиент
 * вправе знать, с кем именно: ФИО, налоговый статус и ИНН — это тот минимум,
 * который проверяет и платёжная система при подключении приёма платежей.
 * Формулировка «реквизиты предоставляются по запросу» для продажи услуги
 * не годится.
 *
 * Значения подставляются во все документы отсюда — оферту, условия, политику,
 * страницу платных услуг и квитанцию. Заполнять в одном месте, здесь.
 *
 * Пока поля пустые, страницы показывают прежнюю формулировку про запрос —
 * пустых строк «ИНН:» на сайте не появится.
 */
export const OPERATOR: { fullName: string; status: string; inn: string } = {
  // ФИО полностью, как в паспорте.
  fullName: 'Антонов Олег Валерьевич',

  // Налоговый статус. Для самозанятого менять не нужно.
  status: 'самозанятый, плательщик налога на профессиональный доход (НПД)',

  // ИНН, 12 цифр. Контрольные цифры проверены 14.08.2026 — сходятся.
  inn: '236411027170',
};

/** Заполнены ли реквизиты — от этого зависит, что показывать в документах. */
export const hasOperatorDetails = OPERATOR.fullName !== '' && OPERATOR.inn !== '';

export const mailtoUrl = `mailto:${SITE.email}`;
