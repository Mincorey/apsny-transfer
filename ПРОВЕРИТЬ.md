# Задания на ручную проверку

Создан 10.08.2026 по итогам аудита `AUDIT_2026-08-10.md`.
Отмечайте: `[ ]` — не проверено, `[x]` — проверено и работает, `[!]` — проверено, есть проблема.

---

## A. Регрессия после применения SQL-патчей (разделы 8.1–8.7 аудита)

Эти сценарии обязательно прогнать **после** каждого патча — патчи урезают права, что-то могло сломаться.

### A1. Поездки
- [ ] Создание поездки «Я ЕДУ» (роль водителя) — проходит все шаги мастера
- [ ] Создание поездки «Я ВЕЗУ» (роль пассажира) — проходит все шаги мастера
- [ ] Черновик появляется в «Мои поездки» со статусом «не опубликована»
- [ ] Кнопка публикации переводит черновик в «активна»
- [ ] Удаление неоплаченного черновика (`delete_unpaid_draft`) работает
- [ ] **Редактирование** поездки владельцем (маршрут, дата, комментарий, места) — не сломалось патчем 8.1
- [ ] Отмена своей активной поездки работает
- [ ] Отмена **чужой** поездки — должна давать ошибку «Отменить поездку может только её создатель»

### A2. Аукцион
- [ ] Ставка через кнопку — проходит, цена меняется
- [ ] Ставка ниже шага — отклоняется с понятным текстом
- [ ] Ставка на свою поездку — отклоняется
- [ ] Две ставки подряд от одного — отклоняется
- [ ] Кнопка «Согласиться с ценой» работает (после патча 8.4)
- [ ] «Согласиться с ценой» после истечения времени — отклоняется
- [ ] Досрочное закрытие аукциона владельцем работает
- [ ] Аукцион закрывается автоматически по истечении времени (cron, ждать до 1 мин)
- [ ] Победителю приходит уведомление «Вы выиграли аукцион», проигравшим — «выбрали другого»

### A3. Профиль
- [ ] Редактирование имени, Telegram, WhatsApp, MAX — сохраняется (не сломалось патчем 8.2)
- [ ] Переключатели приватности (показывать телефон / TG / WA / MAX) — сохраняются
- [ ] Загрузка аватара работает
- [ ] Добавление и активация автомобиля работает, лимит машин срабатывает
- [ ] Рейтинг и число поездок **отображаются** корректно (записывать их клиент больше не может — это нормально)

### A4. Сообщения
- [ ] Отправка сообщения в чате поездки
- [ ] Отправка личного сообщения (диалог)
- [ ] Сообщение помечается прочитанным при открытии (не сломалось патчем 8.6)
- [ ] Счётчик непрочитанных обновляется

### A5. Отзывы
- [ ] После завершённой поездки пассажир может оценить водителя
- [ ] Повторный отзыв по той же поездке — отклоняется
- [ ] Рейтинг водителя пересчитался

---

## B. Проверка закрытых уязвимостей (нужен инструмент вроде Postman/curl)

Все запросы — с anon-ключом и JWT обычного пользователя. **Все должны падать.**

- [ ] `PATCH /rest/v1/rides?id=eq.<свой черновик>` с телом `{"status":"active"}` → **должно быть отказано**
- [ ] `PATCH /rest/v1/users?id=eq.<я>` с телом `{"rating":5,"trips_count":999}` → **должно быть отказано**
- [ ] `POST /rest/v1/rpc/cancel_ride` с чужим `p_ride_id` → **ошибка «только создатель»**
- [ ] `POST /rest/v1/rpc/finish_auction` → **ошибка прав доступа**
- [ ] `POST /rest/v1/bids` (прямая вставка ставки) → **должно быть отказано**
- [ ] `PATCH /rest/v1/direct_messages` с телом `{"content":"взломано"}` → **должно быть отказано**
- [ ] `GET /rest/v1/users?select=phone,email` → **должно быть отказано** (проверено 10.08 — уже отказывает)
- [ ] `GET /rest/v1/payments?select=*` → пустой список или отказ

---

## C. Финансовый контур ЮMoney (перед включением оплаты)

- [ ] `YOOMONEY_NOTIFICATION_SECRET` задан в секретах Edge Functions
- [ ] URL вебхука прописан в настройках ЮMoney
- [ ] Кнопка «Протестировать» в кабинете ЮMoney возвращает 200 OK
- [ ] Публикация с формой оплаты: редирект на ЮMoney происходит, сумма 100 ₽, метка передаётся
- [ ] **Боевая оплата 100 ₽** → `payments.status` стал `paid`
- [ ] После оплаты `rides.status` стал `active`, таймер аукциона запустился
- [ ] Квитанция пришла в Telegram админу
- [ ] Страница `/receipt/:rideId` открывается у владельца и показывает верные данные
- [ ] Страница `/receipt/:rideId` **не** открывается у чужого пользователя
- [ ] **Тест недоплаты:** платёж 99 ₽ → статус `underpaid`, поездка НЕ публикуется, админу приходит предупреждение
- [ ] **Тест идемпотентности:** повторная отправка того же уведомления → второго списания/публикации не происходит
- [ ] `publish_ride_free` больше не вызывается из фронтенда
- [ ] `REVOKE EXECUTE ON publish_ride_free FROM authenticated` применён
- [x] Тексты: нигде не осталось «Platega» (проверено 10.08.2026 — оферта, Условия, Политика, «Платные услуги», квитанция, страница оплаты, документ для модерации)
- [x] На странице статуса больше нет надписи «Оплата получена!», когда денег не было (10.08.2026 — страница `/payment` переписана)
- [ ] После подключения ЮMoney: переключить `PAYMENTS_ENABLED` в `src/lib/publishRide.ts` на `true` — тогда кнопка на `/payment` должна вести на форму оплаты, а не публиковать бесплатно
- [ ] После подключения ЮMoney: убрать со всех кнопок публикации пометку «сейчас бесплатно» (зачёркнутый тариф в `CreateTrip`, `Feed`, `MyTrips`)

---

## D. Страницы и навигация (сквозной проход)

Публичные (без входа):
- [ ] `/` — стартовая, кнопки «Я ЕДУ» / «Я ВЕЗУ» срабатывают с первого клика
- [ ] `/rides/passenger` — лента предложений
- [ ] `/rides/driver` — лента запросов
- [ ] `/trips/:id` — карточка поездки, контакты скрыты
- [ ] `/about`, `/contacts`, `/privacy`, `/terms`, `/offer`, `/paid-services`
- [ ] `/login` — вход и регистрация
- [ ] Несуществующий адрес → страница 404
- [ ] Форма обратной связи на `/contacts` отправляется, сообщение приходит в Telegram

С авторизацией:
- [ ] `/` — лента, поиск, сортировка, вкладки
- [ ] `/create`, `/profile`, `/my-trips`, `/ratings`
- [ ] `/messages`, `/messages/:rideId`, `/conversations`, `/conversations/:id`
- [ ] `/users/:id` — чужой профиль
- [ ] Выход из аккаунта, редирект на `/login`
- [ ] Открытие `/login` уже авторизованным → редирект на `/`

Мобильная версия (проверить на реальном телефоне):
- [ ] Нижнее меню, боковое меню, верхняя панель
- [ ] Мастер создания поездки помещается на экран
- [ ] Выбор даты и времени работает пальцем
- [ ] Чат не «прыгает» при появлении клавиатуры

---

## E. После переноса на VDS

- [ ] Сайт открывается по новому домену, SSL валиден
- [ ] Прямой переход на `/trips/<id>` (не с главной) отдаёт страницу, а не 404 — SPA-fallback nginx
- [ ] Вход и регистрация работают (новый JWT-секрет)
- [ ] Аватары и фото машин отображаются (Storage перенесён)
- [ ] Все 5 cron-задач выполняются (задача 3 — намеренно отключена)
- [ ] Telegram-уведомления работают (Vault-секреты перенесены)
- [ ] Вебхук ЮMoney по новому адресу отвечает 200 OK
- [ ] OG-картинка подтягивается при отправке ссылки в мессенджер (проверить новый домен в мета-тегах)
- [ ] `robots.txt` и `sitemap.xml` содержат новый домен
- [ ] Часовой пояс: поездка, созданная на 20:00, завершается в верное время
- [ ] Бэкап снят **и восстановлен** на тестовой копии
