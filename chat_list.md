# Краткая выжимка чатов по проекту APSNY-TRANSFER

Файл ведётся, чтобы в начале нового чата быстро вспомнить, о чём мы говорили и что делали.
Новые записи добавляются сверху.

---

## 10.08.2026 — Синхронизация с GitHub + полный предпродакшн-аудит

**Что просил пользователь:** сверить локальную папку с GitHub и подтянуть актуальное; провести самый полный аудит перед продакшном (код, функции, БД, все сценарии, кнопки, меню, страницы, финансовая часть); подготовиться к переносу на VDS; собирается подключать ЮMoney.

**Решения пользователя в этом чате:**
- Локальные изменения сбросить на состояние GitHub **без бэкапа** (выбрано осознанно)
- Целевая платёжная система — **ЮMoney** (возврат с Platega)

**Сделано:**
1. Локальная папка синхронизирована: `git reset --hard origin/main` → коммит `fa8951d`. Отставала на 6 коммитов. На GitHub ничего не отправлялось.
2. Проведён полный аудит → `AUDIT_2026-08-10.md` (656 строк).
3. Восстановлен из облака Supabase исходник `supabase/functions/yoomoney-webhook/index.ts` — он был удалён из репозитория, но функция при этом работала в проде.
4. Созданы `ПРОВЕРИТЬ.md` (чеклист ручных проверок) и `MOVING_CHECKLIST.md` (перенос на VDS).

**Главные находки (подробности — в аудите):**
- 🔴 Пользователь может сам переписать `rides.status` с `draft` на `active` → публикация в обход оплаты. **Проверено реальным тестом в БД (откачено).**
- 🔴 Пользователь может поставить себе `rating=5.0` и `trips_count=999`. **Проверено тестом.**
- 🔴 `cancel_ride` не проверяет владельца → можно отменить любую чужую поездку
- 🔴 `finish_auction` открыта пользователям → можно досрочно закрыть чужой аукцион
- 🔴 `accept_current_price` принимает `p_bidder_id` параметром → ставка от чужого имени
- 🟠 Интерфейс пишет «Оплата получена!», когда денег не было
- 🟠 Весь текст сайта (включая оферту) говорит про Platega, а подключается ЮMoney
- ✅ Хорошо: приватность контактов, `publish_ride_paid`, `place_bid`, сборка без ошибок, подпись вебхука соответствует актуальной документации ЮMoney

**Что дальше:** двигаться по разделу 9 аудита («Рекомендуемый порядок работ»). Первый шаг — применить SQL-патчи 8.1–8.6 на ветке БД и прогнать `ПРОВЕРИТЬ.md`.

**Важно помнить:** патчи в аудит только **описаны**, к боевой базе **не применялись**. Никакие данные в проде не менялись.
